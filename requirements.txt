# ── Core ML — TensorFlow stack ─────────────────────────────────────
tensorflow
tensorflow-probability

# ── Conformer model ────────────────────────────────────────────────
# Có thể cài từ PyPI (nếu Python 3.6-3.8) hoặc từ GitHub (Python >=3.9):
#   pip install audio_classification_models
# hoặc:
#   pip install git+https://github.com/awsaf49/audio_classification_models
audio_classification_models @ git+https://github.com/awsaf49/audio_classification_models

# ── Audio ──────────────────────────────────────────────────────────
# tensorflow-io không cần nữa (đã thay thế bằng librosa + tf.signal)
numpy
librosa==0.10.1
soundfile==0.12.1

# ── ASR & NLP ──────────────────────────────────────────────────────
# Cài torch CPU (tránh kéo bản CUDA ~2GB):
#   pip install torch==2.10.0 torchaudio==2.10.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
torchaudio==2.10.0
transformers==4.35.0
datasets==2.14.6
sentencepiece==0.1.99

# ── Fact-checking / Search ─────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.2
newspaper3k==0.2.8
feedparser==6.0.10

# ── UI ─────────────────────────────────────────────────────────────
gradio==3.50.2

# ── Utils ──────────────────────────────────────────────────────────
tqdm==4.66.1
Pillow==10.0.0
matplotlib==3.7.2
scikit-learn==1.3.0
pandas==2.0.3
